package testefuncao03;

public class TesteFuncao03 {
	public static void main(String[] args) { // public torna o metodo publico, static você não precisa transformar em
												// objeto para utiliza-lo, void o meu método não vai me retornar um
												// valor, e args é um vetor de String
		System.out.println("Vai começar a contagem");
		// System.out.println(Operacoes.contador(1, 5));// escrever na tela o resultado
		// do contador

		// a classe Operacoes consegue acessar o método contador, porque ele é estático,
		// se ele não fosse estático a única maneira de conseguir acessa-lo seria por
		// meio da variável de instância criando um novo objeto do tipo operações

		Operacoes operacoes = new Operacoes(); // criei um objeto do tipo Operacoes
		operacoes.contador(1, 5);
	}
}
