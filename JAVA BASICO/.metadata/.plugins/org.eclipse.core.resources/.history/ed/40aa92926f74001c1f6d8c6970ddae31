package testefuncao03;

public class Operacoes {

	String contador(int i, int f) {// método que irá me retornar uma String
		String s = "";
		for (int c = i; c <= f; c++) {
			s += c + "";
		}
		return s;
	}
}
